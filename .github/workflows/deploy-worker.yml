name: Deploy worker

# workers/api/ 또는 shared/ 변경이 main에 푸시되면 Cloudflare Worker 배포
on:
  push:
    branches: [main]
    paths:
      - 'workers/api/**'
      - 'shared/**'
      - '.github/workflows/deploy-worker.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Cloudflare Worker 배포
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workers/api
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: workers/api/package-lock.json

      # workers/api 코드는 ../../shared/db/schema 를 import하고, shared/ 는 루트
      # node_modules에서 drizzle-orm을 찾는다(루트는 npm workspace가 아님). 루트도
      # 설치하지 않으면 wrangler 번들 단계에서 "Could not resolve drizzle-orm"로 실패.
      - name: Install (root — shared/ 의존성)
        run: npm ci
        working-directory: .

      - name: Install
        run: npm ci

      - name: Deploy
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Summary
        run: |
          {
            echo "### Cloudflare Worker 배포 완료"
            echo ""
            echo "Worker: \`weave-story-api\`"
            echo "커밋: $(git log -1 --pretty='%s (%h)')"
          } >> "$GITHUB_STEP_SUMMARY"
